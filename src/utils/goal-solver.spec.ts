import partialToCompleteGoal from './goal-solver';
// // import { GoalData } from '../models/Goal';

describe('partialToCompleteGoal', () => {
  it('always passes', () => {expect(true).toBe(true)});
  // describe('with a solvable goal', () => { // "solvable" is defined as having two of the three attributes set, allowing the third to be calculated
  //   xit('solves goals', () => {
  //     expect(true).toBe(false);
  //   })
  // });
});
